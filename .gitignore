# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.coverage
htmlcov/

# Environment / secrets
.env
.env.*
*.pem
*.key
credentials.json
secrets.json
*token*
!**/tests/**token*

# Editors / OS
.vscode/
.idea/
.DS_Store

# Local machine-specific config (per-checkout paths, not portable)
local_paths.json

# Evidence Package: raw staging artifact from the M4 search phase (not
# part of SPEC.md's canonical evidence_run/evidence_log output schema)
evidence_package/output/_m4_staging_*.json

# DocOps Protocol: vendored from the ToolTempest commit pinned in
# .tooltempest.lock via scripts/sync-tooling.sh -- not authored here,
# so not tracked here. Source of truth: ToolTempest ADR-0001.
/scripts/doc_sync.py
/scripts/doc_sync_tier2.py
/schemas/execution-record.schema.json
