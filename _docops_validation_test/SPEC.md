# DocOps Protocol validation fixture (temporary)

This directory exists only to validate the DocOps Protocol (ToolTempest
ADR-0001) against the real article-pipeline git hooks, without touching
real project documentation (evidence_package/SPEC.md, context_layer/SPEC.md).
Removed once validation is complete.
