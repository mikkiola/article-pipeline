# 0002 — Virality as success metric (superseded)

## Status
Superseded by 0004.

## Context

An early attempt to define Article Pipeline's business goal considered
platform engagement/virality as the success metric, reversing an even
earlier position ("engagement is not the metric").

## Decision

Virality was adopted as the metric — briefly. Reversed in the same
session before any implementation, replaced by the core/adaptive-layer
model (0004).

## Consequences
None — reversed before reaching code. Recorded here specifically because
Immutable Lineage requires reversed decisions to remain visible, not be
deleted.

## Validation
Not applicable — decision never reached implementation.

## Source
Explicit owner decision, reversed same session.
