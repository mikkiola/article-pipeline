# Article Pipeline — Architecture

State as of 2026-09-25. Every fact is a table cell — no prose
sections. Rationale for any decision: `docs/adr/`. Plan:
`docs/ROADMAP.md`. Tasks: `docs/BACKLOG.md`. This document does not
duplicate what those three own — it states current state and
dependencies only.

| Component | Status | Depends on | Validation | Commit |
|---|---|---|---|---|
| Atom Selector | Implemented | Brain (`02_Cards/`) | Tested on real data | `brain.git: bd7b784`, bugfix `97b87f7` (vendored copy; not migrated to single source) |
| `graph_reader.py` | Implemented | Atom Selector | Tested on real data | `brain.git: e7fbc45` (vendored copy; not migrated to single source) |
| Claim Extraction | Implemented | Atom Selector, `graph_reader.py` | 4 immutable pilot runs, manually verified | `46d1a41` |
| Context/causal-structure layer | Implemented | Claim Extraction | M1-M5 complete; context-loss hypothesis not established (see corrective ADR for this milestone). Note, 2026-09-14: `context_layer/enrich.py` — which implemented M1 specifically (the mechanical enrichment transform; M2-M5 were experiments run using it, results in `context_layer/experiment_*.json`) — was deleted this session as confirmed dead/unused code (never imported by anything, no CLI entrypoint, no workflow reference; git history preserves it). This milestone's completed work and its already-produced outputs (consumed by Evidence Package's context-enriched reruns, see row below) are unaffected by the file's removal | `792feb0`..`d2050de` |
| Evidence Package | Implemented | Claim Extraction output | 5 live Claims tested, 4/5 unverifiable, 1 verified (context-enriched rerun; supersedes earlier 5/5 unverifiable result) | `2fe0aac..29f716f`; rerun `2b7c8a6` |
| Strategy Layer | Collector-sourced pipeline complete (two-dimension `CanonicalUnit` contract — `integrity_status`/`corroboration_status`, see `docs/adr/0047-two-dimension-verification-model.md`; Collector adapter with a real ancestry integrity check — the branch-tip `head_sha` Collector recorded at scan time is verified through the GitHub compare API (valid only if it is the branch tip or an ancestor of it), with no local checkout, one shared path for weekly manifests and daily briefs, fail-closed, and each unit carrying only its own repository's commit messages (see `docs/adr/0056-collector-integrity-by-github-ancestry-and-per-repo-commit-messages.md`; a zero-commit repo verifies only that its repo, branch and SHA exist); CI-enforced Anti-Corruption-Layer boundary — import-linter forbidden-import contract, grep-based path-hardcoding lint, synthetic alien-source contract test, all independently mutation-tested, wired into `.github/workflows/strategy-layer-boundary-ci.yml`, see `docs/adr/0048-source-independence-acl-architecture.md`); Brain's own adapter not yet built | Context/causal-structure layer, Collector | 156/156 (`strategy_layer/` 99 + `author/` 57); real end-to-end runs against live `manifest_*.json` and `daily_brief_*.json` data (not synthetic); the ancestry check run against the live GitHub API on 2026-09-25 with the owner's own token (the workflow's App token is not yet exercised) | `c912c28`..`228d251` |
| Author | Implemented — MVP pilot; daily LinkedIn post generation implements its accepted content-voice specification | Strategy Layer's verdict — both LinkedIn and Habr now consume it via `AuthoringContext` (`docs/adr/0045-post-classification-authoring-context.md`) and the Habr multi-claim digest (`docs/adr/0046-habr-multi-claim-digest.md`); no longer fed by `collector/data/manifest_<date>.json` directly — the single-source pilot's original direct-read architecture (`docs/adr/0043-author-mvp-single-source-pilot.md`) has been extended past on this point, not superseded (no later ADR sets that file's `supersedes` field; it remains Accepted exactly as originally written, per Immutable Lineage) | 5 plain-assert tests passing (`author/test_pipeline.py`); 2 real Markdown drafts generated (Habr RU, LinkedIn EN) from Collector's real manifest data, not synthetic. `daily_linkedin_author.py`: 18/18 tests passing (`author/test_daily_linkedin_author.py`); its content-voice specification validated via two real API runs against `daily_brief_2026-09-03.json` | `5d439b5` (pilot); `89bc808` (content-voice specification); `d0bc3aa`, `242a2f5` (routed through Strategy Layer's verdict) |
| Publication Registry | Schema + writer implemented (SPEC.md Milestone M1 only); `content_id` is caller-supplied, not minted by the Registry (`docs/adr/0053-content-id-ownership-moves-to-caller.md`); LinkedIn Auto-Publish (M2, below) is now wired as a real caller, not yet exercised against a real LinkedIn token — M1's own done-when ("one real publication record... from a real M2 run") is not yet met | None — `publication_registry/contract.py`/`writer.py` import no other component's code (pydantic only) | 19/19 unit tests passing (`publication_registry/test_contract.py`+`test_writer.py`); covers all four `write_record()` outcomes (fresh write, unreadable-existing-file conflict, idempotent retry excluding timestamps, field-disagreement conflict) against caller-supplied `content_id` values — no real publication event exists yet | `13000a9`, `8cfe791` (superseded), `d9a7f9f` (content_id ownership decision) |
| LinkedIn Auto-Publish (M2, bootstrap-gated) | Core capability implemented, split from `SAFETY_PAUSE`'s trigger and proactive token-expiry alerting — both blocked on M3 (Owner Verdict)/M5 (Telegram bot), neither started (`docs/adr/0054-m2-ships-core-linkedin-publishing-without-safety-pause-trigger-or-proactive-credential-alerting.md`): scheduled workflow, bootstrap gate wiring (`strategy_layer/pre_filter.py`'s real `classify_unit()`/`check_gate_condition()`, whose integrity input is the GitHub-API ancestry check of Collector's recorded `head_sha` — the workflow's App token reaches the step as `GITHUB_APP_TOKEN`, no local checkout of the repos a brief names; not yet run with that token in a scheduled run), LinkedIn API client (`POST /rest/posts`), fail-closed token preflight, Publication Registry integration. `SAFETY_PAUSE`'s `CLOSED`/`OPEN` state is defined and checked but has no writer — its trigger is M3's Owner Verdict, not built here. Not yet exercised against the real LinkedIn API or a real scheduled run: no `LINKEDIN_ACCESS_TOKEN` (or the other required secrets — `LINKEDIN_ACCESS_TOKEN_EXPIRES_AT`, `LINKEDIN_PERSON_URN`, `ANTHROPIC_API_KEY_DAILY_AUTHOR`, `GH_APP_ID`/`GH_APP_PRIVATE_KEY`) exists yet on this repo (confirmed via `gh secret list --repo mikkiola/article-pipeline`, not assumed) — M2's own done-when ("one real, fully automatic LinkedIn post on real data") is not met | Strategy Layer (`pre_filter.py`'s bootstrap gate), Author (`daily_linkedin_author.py`), Publication Registry, LinkedIn's REST API (external, not yet called for real) | 39/39 unit tests passing (`linkedin_publisher/`); every LinkedIn/Anthropic API call mocked, no live call made; `strategy_layer`/`author`/`publication_registry`'s own suites (99+57+27) passing | `eac99d0` |
| Quality Gate | Not started | Author | — | — |
| Platform Adapter | Not started | Quality Gate | — | — |
| Experiment Log | Not started | Platform Adapter | — | — |
| ToolTempest DocOps Protocol (pre-commit/pre-push hooks + Drift Warning) | Implemented | `mikkiola/tooltempest` (pinned via `.tooltempest.lock`) | Regression-tested: HARD BLOCK (UNKNOWN-pattern), crash-vs-content-problem messages, Drift Warning — see ToolTempest's own `docs/adr/` and this repo's `docs/adr/`; validates only this repo's `SPEC.md` inline `## Milestones` pattern — this repo's own `scripts/verify.py` stopped recognizing a paired `CHECKPOINT.md` as a distinct pattern months before ToolTempest's own `doc_sync.py` removed CHECKPOINT.md handling on its side too, so that support had no live target in this repo either before or after ToolTempest's change | `.tooltempest.lock` pinned at `d35ff6859fb35bbd0e598b94ec141c0b51412881`; Drift Warning `404c24c`; pin bump `dcbd1684d9736aebe3edadedfa7383dc4f09fd93` |
| ToolTempest Tier 2 doc-sync (snapshot, diff, role-gated apply, CLI) | Implemented | ToolTempest DocOps Protocol | Tested via scratch scenarios against real git repos, Stage 2-4: snapshot/diff generation; atomic rollback on rejection; dirty-pre-invocation-state preservation; constrained non-interactive mode; invalid-input-as-rejection; exception-mid-flow rollback; CLI JSON validation (keys outside `TIER2_DOCS`, missing file, malformed JSON) — see ToolTempest's own `docs/adr/`. Vendored as `scripts/doc_sync_tier2.py` (gitignored, same convention as `doc_sync.py`). Contributor-governance workflow built on top (GitHub Actions, PR-as-confirmation) is a separate, not-yet-implemented design — see this repo's `docs/adr/` | `doc_sync_tier2.py` at `mikkiola/tooltempest@5fb62a9`; vendored into this repo `f51c528` |
| ToolTempest CLI adapter + discovery | Not started | ToolTempest DocOps Protocol | — | — |

## Repositories

| Repo | Contains |
|---|---|
| `lyolich777ka/brain.git` | Brain (Obsidian graph), Drift/Collision Engine, original Atom Selector |
| `mikkiola/article-pipeline` | This project's code + `docs/adr/` |
| `mikkiola/tooltempest` | Shared tooling (`/spec`, `/verify`, `drift-control.md`) |

## Models used by this project

| Component | Model or service |
|---|---|
| Claim Extraction | Interactive, Claude Code |
| Evidence Package | Linkup API (search/retrieval, not a language model) |
